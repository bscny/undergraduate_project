## Notice

git tracks small files, larger files will only be available in the local folder, the folder structure should look like:

```
assets
├── action_lists (optional)
│   └── The-action_lists-of-The-Flight-Log
├── flight_logs
│   └── The-Name-of-The-Flight-Log
└── large_files
    ├── airsim_frames
    ├── image2parse
    └── videos
```